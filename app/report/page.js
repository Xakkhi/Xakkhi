import ReportForm from '../../components/ReportForm';

export const metadata = {
  title: 'Report an Issue — Xakkhi সাক্ষী',
  description: 'Report a civic issue in Dibrugarh anonymously. Takes 60 seconds.',
};

export default function ReportPage() {
  return <ReportForm />;
}
